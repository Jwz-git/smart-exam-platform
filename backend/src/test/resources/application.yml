spring:
  datasource:
    url: jdbc:h2:mem:smart_exam_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  sql:
    init:
      mode: always
app:
  security:
    jwt-secret: test-only-secret-that-is-longer-than-32-bytes
    access-token-minutes: 60
