# AGENTS.md

本文件是仓库内所有 AI 编程助手的执行规范，也是项目现状的唯一简明台账。开始任务前必须完整阅读本文件和 `plan.md`；发生代码、配置、文档或项目状态等实质变更后，必须更新本文件中的现状、阶段、验证证据和下一步。纯只读分析不改写台账。

## 1. 项目目标

开发一个实际由单人完成的 Java Web 在线题库与组卷系统，优先打通以下 MVP 闭环：

> 教师维护题库 → 创建并发布试卷 → 学生参加考试 → 系统判客观题 → 教师批阅主观题 → 公布并查看成绩

详细范围、业务规则、排期和验收数据以 `plan.md` 为准。课程要求以 `软件开发实践2_文字整理.md` 为准。若两者冲突，先指出冲突并请求用户决定，不得自行扩大范围。

## 2. 当前项目现状

> 每次完成实质性工作后更新本节。只记录已由文件、命令或测试证明的事实，不把计划写成已完成。

| 项目 | 当前状态 | 可验证依据 |
|---|---|---|
| 仓库 | 已初始化，默认分支为 `main`，已配置 GitHub 远端 | `.git/`、`git remote -v` |
| 需求与计划 | 已完成需求分析并按实际单人开发重排，明确角色、流程、MVP、用例、竞品参考和串行执行方式 | `plan.md`、`docs/requirements-analysis.md` |
| 后端 | 已完成认证、题库、手动组卷与快照、试卷/考试发布、答题、事务交卷、客观题判分和超时自动提交 | `auth/`、`question/`、`exam/`、`ExamFlowIntegrationTest.java` |
| 前端 | 已实现登录、教师题库/组卷/发布、学生考试列表/倒计时/答题/交卷页面；已移除未使用的 Vue 默认 Store、空路由和开发工具插件 | `frontend/src/App.vue`、`frontend/src/api.ts`、`frontend/package.json` |
| 数据库 | 已形成 ER 设计、Flyway V1 结构迁移和 V2 演示账号迁移；无 Docker 的本机初始化路径已验证 | `docs/database-design.md`、`db/migration/`、`database/bootstrap-local.sql` |
| 自动化测试 | 已覆盖健康、认证、题库、组卷、发布、答题、判分、重复交卷、超时交卷和权限路径及前端登录页 | 后端 MockMvc 12 项、前端 Vitest 1 项 |
| 部署 | 已提供并实测一键本机初始化脚本，另保留可选 Compose 数据库 | `scripts/init-local.sh`、`README.md`、`.env.example`、`compose.yaml` |
| 文档 | 已有需求、技术方案、原型、数据库和 REST API 文档；范围、现状、启动说明已拆分为单一职责 | `docs/`、`plan.md`、`README.md` |

**当前阶段：** 阶段 4——阅卷、成绩与核心验收。

**下一步：** 按单人串行排期，9 月 4 日完成完整答卷、阅卷、公布、回看和排名，9 月 5 日完成管理员、题库维护、答题恢复和阶段 4 验收，9 月 6 日真实环境复核后实现 DeepSeek 并完成交付准备。

**最近更新：** 2026-09-03，精简并统一项目文档：计划负责范围与排期，台账负责现状，README 负责启动，其余文档只保留对应领域内容。

## 3. 工作方式

每次任务按以下闭环执行：

1. **核对现状**：运行 `git status --short --branch`，检查目录、构建文件、现有测试和未提交修改；不得覆盖用户已有改动。
2. **定位阶段**：将任务映射到第 4 节的某个阶段，明确本次输入、预期输出和完成标准。
3. **最小实现**：优先交付能验证的最小纵向切片，不提前实现 `plan.md` 中的增强功能。
4. **验证结果**：执行与改动风险相匹配的构建、测试、静态检查或运行验证；不得把未执行的检查写成通过。
5. **同步文档**：代码、配置、接口或运行方式变化时，同步更新相关文档。
6. **更新现状**：修改本文件第 2、4、7 节，记录真实状态、证据、遗留问题和明确下一步。
7. **交付说明**：向用户报告完成内容、验证命令与结果、未完成项和风险。

遇到信息不足时，先区分：已观察事实、合理推断、待确认事项。只有会显著改变架构、数据模型、安全性或项目范围的问题才暂停询问；其他情况采用最小假设继续，并把假设写入决策记录。

## 4. 分阶段实施清单

状态只能使用：`未开始`、`进行中`、`已完成`、`阻塞`。阶段必须满足完成标准后才能标记为已完成。

### 阶段 0：仓库与协作规范

**状态：已完成**

- [x] 初始化 Git 仓库和私有 GitHub 远端
- [x] 创建项目计划 `plan.md`
- [x] 创建 `.gitignore`、`README.md`、`AGENTS.md`
- [x] 创建本地 `CLAUDE.md` 入口并由 `.gitignore` 排除
- [x] 提交并推送当前初始化文件

**完成标准：** 工作区基础文件已提交并推送，远端 `main` 与本地一致。

### 阶段 1：需求、设计与工程骨架

**状态：已完成**

- [x] 固定 Java、Spring Boot、Node.js、Vue 和 MySQL 版本
- [x] 确定前后端目录结构和本地配置策略
- [x] 形成页面/流程原型说明
- [x] 形成数据库 ER 设计与首版迁移脚本
- [x] 形成 REST API 清单和统一响应约定
- [x] 创建可启动的后端、前端工程
- [x] 提供 `.env.example` 或等价的无敏感配置示例

**完成标准：** 前后端可独立启动，数据库可从迁移脚本初始化，接口契约明确且可用于串行联调。

### 阶段 2：认证、权限与题库

**状态：已完成**

- [x] 实现管理员、教师、学生账号和角色权限
- [x] 实现登录、退出、JWT 鉴权和密码安全存储
- [x] 实现知识点与四类题目的增删改查
- [x] 实现题型、难度、知识点筛选
- [x] 覆盖未登录、越权和非法参数测试

**完成标准：** 三类角色可登录；教师可维护并筛选题目；后端权限测试通过。

### 阶段 3：试卷、考试与答题

**状态：已完成**

- [x] 实现手动组卷、分值校验、预览和发布
- [x] 实现考试状态和开放时间规则
- [x] 实现学生答题、倒计时、主动与超时交卷
- [x] 实现客观题判分
- [x] 用事务和唯一约束防止重复提交

**完成标准：** 可以稳定演示“建题—组卷—发布—答题—交卷”，且客观题分数与验收数据一致。

### 阶段 4：阅卷、成绩与核心验收

**状态：进行中**

- [ ] 实现简答题阅卷和总分汇总
- [ ] 实现教师完整答卷查看、成绩公布、学生本人答卷回看和教师统计
- [ ] 实现教师完整排名与学生本人名次，同分采用 `1、2、2、4`
- [ ] 补齐题目/知识点维护页面、管理员页面和答题刷新恢复
- [ ] 执行 `plan.md` 中全部核心验收用例
- [ ] 保存测试输出、接口响应或截图证据
- [ ] 修复所有阻塞主流程的缺陷

**完成标准：** `plan.md` 的 9 项核心验收用例全部通过，主流程无阻塞缺陷。

### 阶段 5：增强功能

**状态：未开始**

只有阶段 4 已完成且用户仍需要时，才按顺序选择实现：

1. DeepSeek AI 辅助出题（用户已选择，生成草稿后必须人工确认，失败不影响手工流程）
2. 规则自动组卷
3. 错题本与错题重练
4. 成绩分布和题目正确率图表
5. 批量导入与试卷导出
6. 自动保存与异常恢复

每个增强功能应单独定义验收标准，不得降低 MVP 的稳定性。

本阶段可按用户决定跳过，阶段 6 不依赖阶段 5。若用户明确不做增强功能，记录决定后将本阶段标记为“已完成（跳过）”；若选择部分功能，所选功能验收通过且未选择项已记录为不在范围内时，本阶段完成。

### 阶段 6：部署、文档与答辩

**状态：未开始**

- [ ] 提供可重复的数据库初始化与演示数据
- [ ] 提供一条清晰的本地或 Docker Compose 启动路径
- [ ] 从干净环境验证 10 分钟内完成启动
- [ ] 补全 README 的环境、配置、启动、测试和演示账号说明
- [ ] 完成需求、设计、测试、项目报告、截图和答辩材料
- [ ] 检查仓库中不存在密钥、真实密码或无关构建产物

**完成标准：** 新环境可按文档启动并重复完成核心演示，课程要求的源码和文档交付物齐备。

## 5. 工程与安全约束

- 前后端分离，浏览器端不能直接访问数据库。
- 后端必须校验身份、角色、资源归属、输入参数和业务状态；前端校验仅改善体验。
- 密码必须使用适合密码存储的单向哈希；JWT 密钥和数据库密码不得提交到仓库。
- 数据库结构变更必须通过版本化迁移脚本完成，不依赖手工改表。
- 已发布试卷、历史答卷和成绩需保留可追溯性，关联数据不得随意物理删除。
- 交卷及计分必须具备事务边界；同一学生同一考试只能有一份有效答卷。
- 新增依赖前先确认必要性和兼容版本；版本事实以构建文件和官方文档为准。
- 不提交 `target/`、`node_modules/`、构建产物、IDE 配置、日志、上传文件或敏感配置。
- 不为了通过测试删除或弱化有效测试；修复原因而不是掩盖症状。

## 6. 验证要求

根据改动范围执行最低验证：

| 改动 | 最低验证 |
|---|---|
| Markdown / 配置 | `git diff --check`，并检查链接、命令和描述与仓库现状一致 |
| Java 后端 | 编译、单元/集成测试；涉及接口时验证正常、非法参数、未登录和越权路径 |
| Vue 前端 | 类型检查、Lint、单元测试和生产构建；涉及交互时验证成功与失败状态 |
| 数据库迁移 | 在空数据库执行迁移；必要时验证回滚或重建路径 |
| 跨端功能 | 执行完整纵向流程并记录关键请求、响应或截图 |
| 部署 | 从干净环境按 README 启动并执行健康检查 |

若某项验证因环境限制无法执行，必须说明具体限制、未验证范围和用户可运行的准确命令。

## 7. 工作记录

> 新记录置顶。只记录实质变更；每条必须包含日期、内容、验证和下一步。每项原则上只写一句，记录最终结果和关键证据，不复述执行过程；风险仅保留会影响后续工作的内容。

### 2026-09-03：精简并统一项目文档

- **内容：** 删除重复背景、技术架构与临时缺口文档，修正单人执行、DeepSeek、依赖、目录、迁移和 API 描述，将范围、现状、启动与专项设计拆分为单一职责。
- **验证：** 文档引用、过时关键词和实现依赖已扫描，`git diff --check` 通过；课程要求原始整理未改动。
- **下一步：** 后续功能变更只更新对应专项文档、`plan.md` 范围及本台账，避免重复描述。

### 2026-09-03：按单人开发重新制定验收前计划

- **内容：** 将全部已确定功能按单人串行拆到 9 月 4—6 日上午，逐项绑定完成标准，并在阶段 4 通过后安排 DeepSeek，不再假设双人并行。
- **验证：** 排期覆盖 MVP、管理员、完整答卷、排名、答题恢复、DeepSeek、9 项验收和全部交付物，并保持 DeepSeek 在阶段 4 通过后实施；`git diff --check` 通过。
- **下一步：** 9 月 4 日 08:00 从阅卷和完整答卷后端接口开始，测试通过后再接页面。

### 2026-09-03：补充基础功能并清理 Vue 默认内容

- **内容：** 将教师完整答卷、学生公布后回看、排名、题库维护页面和答题刷新恢复补入 P0，清理默认 Store、空路由、未用依赖、开发工具插件、模板 README 与 favicon。
- **验证：** 缺口已纳入 `plan.md` 的功能覆盖表；前端类型检查、Lint、Vitest 1 项和生产构建通过，默认内容关键词扫描及 `git diff --check` 通过。
- **下一步：** 先完成 9 月 4 日阅卷、答卷回看和排名纵向切片，再补管理员、题库维护页面与刷新恢复。

### 2026-09-03：确定引入 DeepSeek AI 辅助出题

- **内容：** 新增教师端 DeepSeek 题目草稿生成，限定人工确认、后端密钥、现有校验复用和故障降级，不用于自动阅卷或改变确定性评分。
- **验证：** 已按 DeepSeek 官方 Chat Completions 与 JSON Output 文档核对接口能力和空内容风险，排期未删除既有功能，`git diff --check` 通过。
- **下一步：** 阶段 4 的 9 项核心验收通过后，实现可替换 DeepSeek 客户端、草稿接口与教师确认页面。

### 2026-09-03：按仅上午开发和 9 月 7 日验收再次重排

- **内容：** 保留全部已确定功能、9 项验收和交付物，将阅卷成绩、管理员与自动化验收、真实环境与材料依次安排到 9 月 4—6 日上午，9 月 7 日只做环境检查和正式验收。
- **验证：** 新排期未删除 `plan.md` 的任何 MVP 功能或交付物，日期与用户可用时间一致，`git diff --check` 通过。
- **下一步：** 9 月 4 日上午完成阅卷、28 分汇总、成绩公布、本人查分和教师统计的完整纵向切片。

### 2026-09-03：按实际进度重排 9 月 3—6 日任务

- **内容：** 将已提前完成的考试主流程记入 9 月 3 日，9 月 4 日集中阅卷成绩闭环，9 月 5 日安排真实环境验收与材料，9 月 6 日仅保留干净环境回归和交付。
- **验证：** 排期已逐项对应当前阶段、9 项核心验收和交付物，`git diff --check` 通过。
- **下一步：** 先实现简答题评分、总分 28 分校验和公布前不可查分。

### 2026-09-03：完成 9 月 4 日考试主流程

- **内容：** 完成手动组卷与快照、分值校验、试卷/考试发布、学生倒计时答题、事务交卷、客观题判分、唯一答卷和后端超时自动提交。
- **验证：** 后端 MockMvc 12 项通过，覆盖四题 40 分试卷、客观题 20 分、重复与超时交卷及权限；前端全套检查通过。
- **下一步：** 实现简答题阅卷、成绩汇总、公布与查询，并执行 9 项核心验收。

### 2026-09-03：复核完成 9 月 3 日基础功能开发

- **内容：** 确认三类角色登录、JWT 鉴权、教师题库 CRUD/筛选及对应页面已实现，9 月 3 日完成标准已满足。
- **验证：** 后端 `mvn test` 9 项通过；前端类型检查、Lint、Vitest 1 项和生产构建通过。
- **下一步：** 按 9 月 4 日计划实现组卷、考试发布、学生答题、交卷与客观题判分。

### 2026-09-02：精简工作记录规范

- **内容：** 工作记录改为每项一句，只保留结果、关键证据和下一步。
- **验证：** `git diff --check` 通过。
- **下一步：** 后续记录沿用精简格式。

### 2026-09-02：完成阶段 2 认证、权限与题库

- **内容：** 完成认证、权限、知识点与四类题目 CRUD/筛选，以及登录和教师题库页面。
- **验证：** 后端 9 项测试和前端全套检查通过；MySQL 9.6 与 Vite 代理纵向流程通过，MySQL 8.4 待回归。
- **下一步：** 实现试卷草稿、手动组卷、分值校验、预览和发布。

### 2026-09-02：完成一键本机初始化

- **内容：** 新增一键初始化脚本，自动生成本机密钥、初始化数据库、安装依赖并验证两端。
- **验证：** 脚本实测成功，Flyway 到 V2、两端检查通过；Node.js 25 有非阻塞引擎警告。
- **下一步：** 其他成员克隆后执行 `./scripts/init-local.sh`。

### 2026-09-02：提前完成认证与基础角色鉴权

- **内容：** 完成三类演示账号、BCrypt、JWT 登录/退出和基础角色鉴权。
- **验证：** 后端 4 项测试及 MySQL 9.6 实际登录通过，MySQL 8.4 待回归。
- **下一步：** 实现题库 CRUD、筛选与权限测试。

### 2026-09-01：补充高保真界面原型总览

- **内容：** 归档登录、教师题库、手动组卷和学生答题高保真原型总览。
- **验证：** PNG 为 1672×941，核心页面和流程入口可见，`git diff --check` 通过。
- **下一步：** 开发以 API 和文字原型为准，完成后补真实截图。

### 2026-09-01：完成设计方案与工程骨架

- **内容：** 固定技术版本，完成设计文档、Flyway V1、数据库初始化脚本及前后端骨架。
- **验证：** 两端测试与构建、MySQL 初始化、健康检查和 Compose 配置通过；MySQL 8.4 待回归。
- **下一步：** 实现账号、JWT 鉴权和题库 CRUD。

### 2026-09-01：补齐 9 月 1 日需求分析交付物

- **内容：** 补齐需求分析的角色、流程、异常规则、验收数据、竞品和范围取舍。
- **验证：** Markdown、相对链接和关键章节检查通过，竞品信息已按官方资料核对。
- **下一步：** 固定技术方案并建立可启动工程骨架。

### 2026-09-01：调整为动态任务认领

- **内容：** 将固定前后端分工改为动态认领或结对，并保留负责人、复核人和证据。
- **验证：** 固定个人指派已移除，`git diff --check` 通过。
- **下一步：** 每日开始前认领任务并记录责任人。

### 2026-09-01：完成 9 月 1 日需求分析

- **内容：** 完成需求分析并将阶段 0 标记完成、阶段 1 标记进行中。
- **验证：** 初始化提交已推送，Markdown 和相对链接检查通过。
- **下一步：** 完成技术方案、原型、迁移、API 清单和工程骨架。

### 2026-09-01：仓库基础文档

- **内容：** 建立仓库基础文档、忽略规则和本地 `CLAUDE.md` 入口。
- **验证：** 文档检查通过，`CLAUDE.md` 已确认被忽略。
- **下一步：** 提交初始化文件并创建工程骨架。

## 8. AI 交付检查表

结束每次任务前逐项确认：

- [ ] 没有覆盖或删除用户未授权修改
- [ ] 实现范围与 `plan.md` 当前阶段一致
- [ ] 已运行并如实报告必要验证
- [ ] README、接口、数据库或部署说明已按需同步
- [ ] 第 2 节项目现状与真实文件一致
- [ ] 第 4 节阶段状态和清单已更新
- [ ] 第 7 节已记录本次证据、遗留问题和下一步
- [ ] 未提交密钥、密码、令牌、个人数据或生成产物
