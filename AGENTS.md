# AGENTS.md

本文件是仓库内所有 AI 编程助手的执行规范，也是项目现状的唯一简明台账。开始任务前必须完整阅读本文件和 `plan.md`；发生代码、配置、文档或项目状态等实质变更后，必须更新本文件中的现状、阶段、验证证据和下一步。纯只读分析不改写台账。

## 1. 项目目标

开发一个两人协作的 Java Web 在线题库与组卷系统，优先打通以下 MVP 闭环：

> 教师维护题库 → 创建并发布试卷 → 学生参加考试 → 系统判客观题 → 教师批阅主观题 → 公布并查看成绩

详细范围、业务规则、排期和验收数据以 `plan.md` 为准。课程要求以 `软件开发实践2_文字整理.md` 为准。若两者冲突，先指出冲突并请求用户决定，不得自行扩大范围。

## 2. 当前项目现状

> 每次完成实质性工作后更新本节。只记录已由文件、命令或测试证明的事实，不把计划写成已完成。

| 项目 | 当前状态 | 可验证依据 |
|---|---|---|
| 仓库 | 已初始化，默认分支为 `main`，已配置 GitHub 远端 | `.git/`、`git remote -v` |
| 需求与计划 | 已完成 9 月 1 日需求分析，明确角色、流程、MVP、用例、竞品参考和动态认领协作方式 | `plan.md`、`docs/requirements-analysis.md` |
| 后端 | 未初始化 | 尚无后端构建文件或源码目录 |
| 前端 | 未初始化 | 尚无前端构建文件或源码目录 |
| 数据库 | 未设计落盘 | 尚无迁移脚本或 schema 文件 |
| 自动化测试 | 未建立 | 尚无测试配置和测试源码 |
| 部署 | 未建立 | 尚无容器或部署配置 |
| 文档 | 已有课程原文、计划、README 和 AI 协作规范 | 仓库根目录 Markdown 文件 |

**当前阶段：** 阶段 1——需求、设计与工程骨架。

**下一步：** 固定最终技术版本和目录结构，形成页面原型、数据库首版迁移与 REST API 清单，并创建可启动的前后端工程骨架。

**最近更新：** 2026-09-01，将两人固定前后端分工调整为按任务动态认领。

## 3. 工作方式

每次任务按以下闭环执行：

1. **核对现状**：运行 `git status --short --branch`，检查目录、构建文件、现有测试和未提交修改；不得覆盖用户已有改动。
2. **定位阶段**：将任务映射到第 4 节的某个阶段，明确本次输入、预期输出和完成标准。
3. **最小实现**：优先交付能验证的最小纵向切片，不提前实现 `plan.md` 中的增强功能。
4. **验证结果**：执行与改动风险相匹配的构建、测试、静态检查或运行验证；不得把未执行的检查写成通过。
5. **同步文档**：代码、配置、接口或运行方式变化时，同步更新相关文档。
6. **更新现状**：修改本文件第 2、4、7 节，记录真实状态、证据、遗留问题和明确下一步。
7. **交付说明**：向用户报告完成内容、验证命令与结果、未完成项和风险。

遇到信息不足时，先区分：已观察事实、合理推断、待确认事项。只有会显著改变架构、数据模型、安全性或项目范围的问题才暂停询问；其他情况采用最小假设继续，并把假设写入决策记录。

## 4. 分阶段实施清单

状态只能使用：`未开始`、`进行中`、`已完成`、`阻塞`。阶段必须满足完成标准后才能标记为已完成。

### 阶段 0：仓库与协作规范

**状态：已完成**

- [x] 初始化 Git 仓库和私有 GitHub 远端
- [x] 创建项目计划 `plan.md`
- [x] 创建 `.gitignore`、`README.md`、`AGENTS.md`
- [x] 创建本地 `CLAUDE.md` 入口并由 `.gitignore` 排除
- [x] 提交并推送当前初始化文件

**完成标准：** 工作区基础文件已提交并推送，远端 `main` 与本地一致。

### 阶段 1：需求、设计与工程骨架

**状态：进行中**

- [ ] 固定 Java、Spring Boot、Node.js、Vue 和 MySQL 版本
- [ ] 确定前后端目录结构和本地配置策略
- [ ] 形成页面/流程原型说明
- [ ] 形成数据库 ER 设计与首版迁移脚本
- [ ] 形成 REST API 清单和统一响应约定
- [ ] 创建可启动的后端、前端工程
- [ ] 提供 `.env.example` 或等价的无敏感配置示例

**完成标准：** 前后端可独立启动，数据库可从迁移脚本初始化，接口契约可供两人并行开发。

### 阶段 2：认证、权限与题库

**状态：未开始**

- [ ] 实现管理员、教师、学生账号和角色权限
- [ ] 实现登录、退出、JWT 鉴权和密码安全存储
- [ ] 实现知识点与四类题目的增删改查
- [ ] 实现题型、难度、知识点筛选
- [ ] 覆盖未登录、越权和非法参数测试

**完成标准：** 三类角色可登录；教师可维护并筛选题目；后端权限测试通过。

### 阶段 3：试卷、考试与答题

**状态：未开始**

- [ ] 实现手动组卷、分值校验、预览和发布
- [ ] 实现考试状态和开放时间规则
- [ ] 实现学生答题、倒计时、主动与超时交卷
- [ ] 实现客观题判分
- [ ] 用事务和唯一约束防止重复提交

**完成标准：** 可以稳定演示“建题—组卷—发布—答题—交卷”，且客观题分数与验收数据一致。

### 阶段 4：阅卷、成绩与核心验收

**状态：未开始**

- [ ] 实现简答题阅卷和总分汇总
- [ ] 实现成绩公布、学生查分和教师统计
- [ ] 执行 `plan.md` 中全部核心验收用例
- [ ] 保存测试输出、接口响应或截图证据
- [ ] 修复所有阻塞主流程的缺陷

**完成标准：** `plan.md` 的 9 项核心验收用例全部通过，主流程无阻塞缺陷。

### 阶段 5：增强功能

**状态：未开始**

只有阶段 4 已完成且用户仍需要时，才按顺序选择实现：

1. 规则自动组卷
2. 错题本与错题重练
3. 成绩分布和题目正确率图表
4. 批量导入与试卷导出
5. 自动保存与异常恢复

每个增强功能应单独定义验收标准，不得降低 MVP 的稳定性。

本阶段可按用户决定跳过，阶段 6 不依赖阶段 5。若用户明确不做增强功能，记录决定后将本阶段标记为“已完成（跳过）”；若选择部分功能，所选功能验收通过且未选择项已记录为不在范围内时，本阶段完成。

### 阶段 6：部署、文档与答辩

**状态：未开始**

- [ ] 提供可重复的数据库初始化与演示数据
- [ ] 提供一条清晰的本地或 Docker Compose 启动路径
- [ ] 从干净环境验证 10 分钟内完成启动
- [ ] 补全 README 的环境、配置、启动、测试和演示账号说明
- [ ] 完成需求、设计、测试、项目报告、截图和答辩材料
- [ ] 检查仓库中不存在密钥、真实密码或无关构建产物

**完成标准：** 新环境可按文档启动并重复完成核心演示，课程要求的源码和文档交付物齐备。

## 5. 工程与安全约束

- 前后端分离，浏览器端不能直接访问数据库。
- 后端必须校验身份、角色、资源归属、输入参数和业务状态；前端校验仅改善体验。
- 密码必须使用适合密码存储的单向哈希；JWT 密钥和数据库密码不得提交到仓库。
- 数据库结构变更必须通过版本化迁移脚本完成，不依赖手工改表。
- 已发布试卷、历史答卷和成绩需保留可追溯性，关联数据不得随意物理删除。
- 交卷及计分必须具备事务边界；同一学生同一考试只能有一份有效答卷。
- 新增依赖前先确认必要性和兼容版本；版本事实以构建文件和官方文档为准。
- 不提交 `target/`、`node_modules/`、构建产物、IDE 配置、日志、上传文件或敏感配置。
- 不为了通过测试删除或弱化有效测试；修复原因而不是掩盖症状。

## 6. 验证要求

根据改动范围执行最低验证：

| 改动 | 最低验证 |
|---|---|
| Markdown / 配置 | `git diff --check`，并检查链接、命令和描述与仓库现状一致 |
| Java 后端 | 编译、单元/集成测试；涉及接口时验证正常、非法参数、未登录和越权路径 |
| Vue 前端 | 类型检查、Lint、单元测试和生产构建；涉及交互时验证成功与失败状态 |
| 数据库迁移 | 在空数据库执行迁移；必要时验证回滚或重建路径 |
| 跨端功能 | 执行完整纵向流程并记录关键请求、响应或截图 |
| 部署 | 从干净环境按 README 启动并执行健康检查 |

若某项验证因环境限制无法执行，必须说明具体限制、未验证范围和用户可运行的准确命令。

## 7. 工作记录

> 新记录置顶。只记录实质变更；每条必须包含日期、内容、验证和下一步。

### 2026-09-01：调整为动态任务认领

- **内容：** 取消成员 A/B 的固定前后端主责和逐日指派，改为两人按当天情况动态认领或结对处理；每项任务仍记录负责人、复核人和完成证据，并同步调整排期、验收责任和风险应对描述。
- **验证：** 执行 `git diff --check`；检索计划中的成员 A/B 和固定主责表述，确认已移除固定个人指派。
- **下一步：** 9 月 2 日开始前由两人认领阶段 1 各项任务，并在任务记录中写明当次负责人和复核人。

### 2026-09-01：完成 9 月 1 日需求分析

- **内容：** 补充 `docs/requirements-analysis.md`，集中记录角色、MVP 边界、核心用例、业务流程、竞品官方资料及范围取舍；核验初始化提交已推送，并将阶段 0 标记为完成、阶段 1 标记为进行中。
- **验证：** `HEAD` 与 `origin/main` 在修改前均为 `17d0326`；文档修改后执行 `git diff --check`，并检查 Markdown 相对链接目标存在。
- **下一步：** 按 9 月 2 日计划固定技术版本和目录结构，形成原型、数据库迁移、API 清单及可启动工程骨架。

### 2026-09-01：仓库基础文档

- **内容：** 建立 `.gitignore`、`README.md`、`AGENTS.md` 和本地 `CLAUDE.md` 入口。
- **验证：** `git diff --check` 通过；`git check-ignore -v CLAUDE.md` 确认该入口由 `.gitignore` 第 2 行忽略；文档描述与当前目录状态一致。
- **下一步：** 提交并推送初始化文件；随后确认技术版本并创建前后端骨架。

## 8. AI 交付检查表

结束每次任务前逐项确认：

- [ ] 没有覆盖或删除用户未授权修改
- [ ] 实现范围与 `plan.md` 当前阶段一致
- [ ] 已运行并如实报告必要验证
- [ ] README、接口、数据库或部署说明已按需同步
- [ ] 第 2 节项目现状与真实文件一致
- [ ] 第 4 节阶段状态和清单已更新
- [ ] 第 7 节已记录本次证据、遗留问题和下一步
- [ ] 未提交密钥、密码、令牌、个人数据或生成产物
